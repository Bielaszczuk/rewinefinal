apiVersion: v1
kind: Secret
metadata:
  name: rewine-backend-secrets
  labels:
    app: rewine-backend
    component: backend
type: Opaque
# =============================================================================
# WARNING: This is a placeholder file!
# =============================================================================
# Do NOT commit real secrets to version control.
#
# In OpenShift/Kubernetes, secrets should be:
#   - Created via sealed-secrets, external-secrets, or vault
#   - Managed through a secrets management tool
#   - Injected via CI/CD pipeline
#
# To create secrets manually (for development):
#   kubectl create secret generic rewine-backend-secrets \
#     --from-literal=SPRING_DATASOURCE_PASSWORD=your_password \
#     --from-literal=JWT_SECRET=your_jwt_secret_min_32_chars
# =============================================================================
stringData:
  # Database credentials (REPLACE WITH REAL VALUES)
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgres-service:5432/rewine"
  SPRING_DATASOURCE_USERNAME: "rewine"
  SPRING_DATASOURCE_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"

  # JWT Secret (minimum 32 characters)
  JWT_SECRET: "REPLACE_WITH_ACTUAL_JWT_SECRET_MIN_32_CHARS"

